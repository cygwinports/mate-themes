inherit mate


src_install() {
	cd ${B}
	cyginstall

	pushd ${D}/usr/share
	# locale not needed, themes conflict with gnome-themes-standard
	rm -fr locale/ themes/{HighContrast,HighContrastInverse,LowContrast}/
	popd
}
